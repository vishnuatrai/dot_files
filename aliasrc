# ruby/rails
alias bes="bundle exec rspec"
alias ber="bundle exec rake"
alias bake="bundle exec rake"
alias be="bundle exec"
alias UVMstart="/Users/vsharm24/src/dot_files/startUVMs"
alias UVMstop="/Users/vsharm24/src/dot_files/stopUVMs"

alias CVMstart="/Users/vsharm24/src/dot_files/startCVMs"
alias CVMstop="/Users/vsharm24/src/dot_files/stopCVMs"

alias fsdp="cd /usr/local/src/federated-data/fs-parent && mvn clean install && cp /usr/local/src/federated-data/fs-app/target/fs-app.war /usr/local/src/wildfly-10.1.0.Final/standalone/deployments/ && cd /usr/local/src/federated-data/fs-app"
alias fsp="/usr/local/src/federated-data"
alias fss="/usr/local/src/wildfly-10.1.0.Final/bin/standalone.sh --debug"



alias gbr="git remote prune origin && git branch -r"

# filesystem
alias ..="cd ../"
alias ...="cd ../.."
alias ....="cd ../../.."
alias o=$EDITOR
alias dotf="cd $HOME/.dotfiles"
alias mkdir="mkdir -p"
alias s="sake"
# services
alias nick="mplayer ~/Music/Theme\ Music/528491.wav > /dev/null 2>&1"

#git
alias g="git"
alias gst='git status'
alias gl='git pull'
alias gup='git fetch && git rebase'
alias gp='git push'
alias gd='git diff | mate'
alias gdv='git diff -w "$@" | vim -R -'
alias gc='git commit -v'
alias gca='git commit -v -a'
alias gb='git branch'
alias gba='git branch -a'
alias gcount='git shortlog -sn'
alias gcp='git cherry-pick'
alias grst='git reset --hard HEAD'
alias gf="git fetch"
alias gsi="git submodule init"
alias gsu="git submodule update"

alias s="sake"

alias psr="ps axu|grep ruby"
alias ..2="cd ../.."
alias ..3="cd ../../.."
alias r="rake"
alias screen="screen -h 5000"
alias emacs="open -a Emacs.app"
alias em="/Applications/Emacs.app/Contents/MacOS/bin/emacsclient --no-wait"
#alias ls="ls -G --color=auto"      #Ubuntu
alias ls="ls -G"                    #Mac OS 

# misc.
alias timestamp="date +%Y%m%d%H%M%S"
alias python=python3
alias pip=pip3

export JAVA_8_HOME=$(/usr/libexec/java_home -v1.8)
export JAVA_7_HOME=$(/usr/libexec/java_home -v1.7)
export JAVA_HOME=$JAVA_8_HOME

export JAVA_11_HOME=$(/usr/libexec/java_home -v11)
#alias j11='export JAVA_HOME=$JAVA_11_HOME; java -version'
alias j11='export JAVA_HOME=$JAVA_11_HOME; export CLASSPATH="$JAVA_HOME/lib"; export PATH="$CLASSPATH:$PATH"; java -version'

alias j8='export JAVA_HOME=$JAVA_8_HOME; java -version'
#alias j8='export JAVA_HOME=$JAVA_8_HOME; export CLASSPATH="$JAVA_HOME/lib"; export PATH="$CLASSPATH:$PATH"; java -version'

alias j7='export JAVA_HOME=$JAVA_7_HOME; java -version'
#alias j7='export JAVA_HOME=$JAVA_7_HOME; export CLASSPATH="$JAVA_HOME/lib"; export PATH="$CLASSPATH:$PATH"; java -version'

alias try_jshell='java11 && java -jar /Users/vsharm24/src/try-artifact-0.2.1.jar'
